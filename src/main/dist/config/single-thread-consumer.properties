bootstrap.servers=localhost:9092
threading.model=single-thread
# Doesn't matter if we are on single thread
num.threads=1
record.processing.time.ms=1
test.topic.input.name=test

# Consumer configs
consumer.group.id=default-group-id
consumer.poll.timeout.ms=300
consumer.poll.retries=100
consumer.session.timeout.ms=20000
consumer.max.poll.interval.ms=2147483647
